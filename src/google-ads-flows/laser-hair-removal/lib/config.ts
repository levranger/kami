// Temporary test flag: hides all pricing and the $149 "Must-Have" offer from
// the LHR booking flow (area prices, offer card, sticky CTA price, review/
// confirmation totals) to see whether a price-free flow affects conversion.
// Flip back to true to fully restore pricing — no other code changes needed.
export const SHOW_PRICING = false;
